# Demo App Design: Support Ticket System

## Purpose

A minimal support ticket app used to record SDD demo videos for the WebConf 2026 presentation. The app must look credible on camera while being trivial to set up and run. No real backend — MSW provides all data.

The baseline app ships with a working ticket list + detail view. The **demo feature** (added during the SDD recordings) is a search filter with debounced input.

## Tech Stack

| Tool           | Version | Purpose                                                                           |
| -------------- | ------- | --------------------------------------------------------------------------------- |
| React          | 19      | UI framework                                                                      |
| TypeScript     | 5.x     | Type safety                                                                       |
| Vite           | 6.x     | Build tool + dev server                                                           |
| Chakra UI      | 3.x     | Component library                                                                 |
| TanStack Query | 5.x     | Server state management                                                           |
| react-router   | 7.x     | Routing (single `/` route — included for provider pattern and demo extensibility) |
| MSW            | 2.x     | Mock API (browser mode)                                                           |
| Vitest         | 3.x     | Unit/integration tests (hooks, utilities, non-visual logic)                       |
| Playwright     | 1.x     | Component testing mode for visual/interaction tests                               |

## Data Model

```ts
interface Ticket {
  id: string
  title: string
  description: string
  status: "open" | "in-progress" | "resolved"
  priority: "low" | "medium" | "high"
  createdAt: string   // ISO 8601
  updatedAt: string   // ISO 8601
  assignee: string
}
```

MSW seeds 15-20 tickets with realistic support content (password resets, billing issues, API errors, feature requests, etc.).

## API Endpoints (MSW)

| Method | Path             | Response   |
| ------ | ---------------- | ---------- |
| GET    | /api/tickets     | `Ticket[]` |
| GET    | /api/tickets/:id | `Ticket`   |

Both return JSON. No pagination needed for the seed data size.

## Layout

Single page, split layout:

```
┌─────────────────────────────────────────────┐
│  Header: "Acme Support"                     │
├──────────────────────┬──────────────────────┤
│  Ticket List (table) │  Detail Panel        │
│                      │                      │
│  Columns:            │  - Title             │
│  - Title             │  - Status badge      │
│  - Status (badge)    │  - Priority badge    │
│  - Priority (badge)  │  - Assignee          │
│  - Created date      │  - Description       │
│                      │  - Created/Updated   │
│  Selected row is     │                      │
│  visually highlighted│  Empty state:        │
│                      │  "Select a ticket"   │
└──────────────────────┴──────────────────────┘
```

- Left panel: ~60% width. Ticket table with clickable rows.
- Right panel: ~40% width. Shows selected ticket detail or empty state.
- Header: app name, no navigation links needed.

## Project Structure

```
demo-app/
├── index.html
├── package.json
├── vite.config.ts
├── vitest.config.ts             # Vitest config (jsdom environment)
├── playwright-ct.config.ts      # Playwright component testing config
├── tsconfig.json
├── src/
│   ├── main.tsx                 # Entry: React root, providers (QueryClient, ChakraProvider, RouterProvider)
│   ├── App.tsx                  # Router setup, single route
│   ├── components/
│   │   ├── Layout.tsx           # Header + horizontal split container
│   │   ├── TicketList.tsx       # Table with row selection, uses useTickets() query
│   │   └── TicketDetail.tsx     # Detail card, uses useTicket(id) query
│   ├── api/
│   │   └── tickets.ts           # TanStack Query hooks: useTickets(), useTicket(id)
│   ├── types/
│   │   └── ticket.ts            # Ticket type definition
│   └── mocks/
│       ├── browser.ts           # MSW setupWorker()
│       ├── server.ts            # MSW setupServer() for Vitest (node)
│       ├── handlers.ts          # Request handlers for /api/tickets
│       └── data.ts              # Seed ticket data array
├── tests/
│   ├── unit/                    # Vitest tests for hooks, utils, data logic
│   │   ├── tickets.test.ts      # useTickets / useTicket hook tests
│   │   └── data.test.ts         # Seed data integrity tests
│   └── components/              # Playwright component tests
│       ├── TicketList.spec.tsx   # List rendering, row selection, badges
│       └── TicketDetail.spec.tsx # Detail display, empty state
├── playwright/
│   └── index.tsx                # Playwright CT mount setup (providers wrapper)
└── public/
    └── mockServiceWorker.js     # Generated by `npx msw init public/`
```

## Component Details

### Layout.tsx
- Chakra `Flex` horizontal split: left (60%) and right (40%)
- Header bar with "Acme Support" text and a subtle background color
- Full viewport height

### TicketList.tsx
- Chakra `Table` component
- Columns: title, status (Badge), priority (Badge), created date (relative or short format)
- Clicking a row calls `onSelect(ticketId)` — selection state lives in parent or URL search param
- Selected row gets a highlight background
- Loading state: Chakra Skeleton rows
- Status badge colors: open=blue, in-progress=yellow, resolved=green
- Priority badge colors: low=gray, medium=orange, high=red

### TicketDetail.tsx
- Receives `ticketId: string | null`
- When null: centered "Select a ticket to view details" message
- When set: fetches ticket via `useTicket(id)`, displays all fields
- Chakra Card with Stack layout for fields

## Acceptance Criteria

### Layout

- [ ] Header displays "Acme Support" app name
- [ ] Page fills full viewport height, no scrollbar on the body
- [ ] Left panel takes ~60% width, right panel ~40%
- [ ] Both panels are visible side by side on load

### Ticket List

- [ ] All seeded tickets render as rows in a table
- [ ] Each row shows: title, status badge, priority badge, created date
- [ ] Status badges use correct colors: open=blue, in-progress=yellow, resolved=green
- [ ] Priority badges use correct colors: low=gray, medium=orange, high=red
- [ ] Clicking a row selects it — row gets a visible highlight
- [ ] Clicking a different row moves the selection
- [ ] While tickets are loading, skeleton/placeholder rows are shown
- [ ] If the API returns an error, an error message is displayed

### Ticket Detail

- [ ] When no ticket is selected, shows centered "Select a ticket to view details" message
- [ ] When a ticket is selected, displays: title, status badge, priority badge, assignee, description, created date, updated date
- [ ] Status and priority badges match the same color scheme as the list
- [ ] While the ticket is loading, a loading indicator is shown
- [ ] If the ticket fetch fails (e.g., 404), an error message is shown

### Data & API

- [ ] MSW intercepts all `/api/tickets` and `/api/tickets/:id` requests — no network errors in devtools
- [ ] Seed data contains 15-20 tickets with a realistic mix of statuses, priorities, assignees, and dates
- [ ] `GET /api/tickets` returns all tickets
- [ ] `GET /api/tickets/:id` returns a single ticket or 404 for unknown ids

### Test IDs

Every assertable element must have a `data-testid`. Required test IDs:

- `layout-header` — header bar
- `ticket-list-table` — the ticket table
- `ticket-list-row-{id}` — each ticket row, keyed by ticket id
- `ticket-list-status-badge-{id}` — status badge per row
- `ticket-list-priority-badge-{id}` — priority badge per row
- `ticket-list-loading` — skeleton/loading state
- `ticket-list-error` — error state
- `ticket-detail-panel` — the detail panel container
- `ticket-detail-empty` — empty state message
- `ticket-detail-title` — ticket title in detail
- `ticket-detail-status-badge` — status badge in detail
- `ticket-detail-priority-badge` — priority badge in detail
- `ticket-detail-assignee` — assignee field
- `ticket-detail-description` — description field
- `ticket-detail-created` — created date
- `ticket-detail-updated` — updated date
- `ticket-detail-loading` — loading state in detail
- `ticket-detail-error` — error state in detail

## TanStack Query Hooks

### useTickets()
```ts
useQuery({
  queryKey: ["tickets"],
  queryFn: () => fetch("/api/tickets").then(r => r.json()),
})
```

### useTicket(id)
```ts
useQuery({
  queryKey: ["tickets", id],
  queryFn: () => fetch(`/api/tickets/${id}`).then(r => r.json()),
  enabled: !!id,
})
```

## MSW Setup

- Browser mode only (`setupWorker`)
- Started conditionally in `main.tsx` before React renders (await `worker.start()`)
- Handlers use an in-memory `tickets` array from `data.ts`
- `mockServiceWorker.js` generated via `npx msw init public/`

## Seed Data

15-20 tickets with varied:
- Statuses: mix of open (majority), in-progress, resolved
- Priorities: mix of all three
- Titles: realistic support topics (e.g., "Cannot reset password", "API rate limit errors on dashboard", "Billing shows wrong plan", "Feature request: dark mode")
- Assignees: 3-4 fictional names
- Dates: spread over the last 2 weeks

## Testing Strategy

Two test layers, each with a clear boundary:

### Vitest (unit/integration — non-visual)

- **Environment:** jsdom
- **MSW:** `setupServer()` from `msw/node` — same handlers as browser, started in `beforeAll` / reset in `afterEach`
- **What to test:**
  - `useTickets()` — returns ticket list, handles loading/error states
  - `useTicket(id)` — returns single ticket, skips fetch when id is null, handles 404
  - `data.ts` — seed data has correct shape, expected count, varied statuses/priorities
- **Selectors:** Use `screen.getByTestId()` — all elements under test must have `data-testid` attributes
- **Wrapper:** A test utility that wraps components in `QueryClientProvider` (fresh client per test) for hook testing via `renderHook`

### Playwright Component Testing (visual/interaction)

- **Mode:** Component Testing (`playwright-ct`) — renders individual components in a real browser, no full app server needed
- **Mount wrapper:** `playwright/index.tsx` provides `ChakraProvider` + `QueryClientProvider` so components render with styles and data
- **MSW:** Use `page.route()` to intercept `/api/*` requests in Playwright — simpler than wiring MSW into the CT browser
- **Selectors:** Use `page.getByTestId()` exclusively — all elements must have `data-testid` attributes (see Acceptance Criteria > Test IDs)
- **What to test:**
  - `TicketList` — renders all tickets, shows correct status/priority badge colors, clicking a row calls onSelect, selected row is highlighted
  - `TicketDetail` — displays all ticket fields when given an id, shows empty state when no ticket selected, badge colors match status/priority
  - `Layout` — header renders app name, split layout has both panels

### Test commands

```bash
npm run test          # Vitest (watch mode)
npm run test:ci       # Vitest (single run, CI)
npm run test:ct       # Playwright component tests
```

## What's NOT in the Baseline

These are intentionally excluded — the search filter is the demo feature:

- **No search/filter** — added during SDD demo recording
- **No create/edit/delete** — not needed for demos
- **No auth** — as specified
- **No dark mode** — default Chakra light theme
- **No deployment** — local dev only
